<Page
    x:Class="Ghenterprise.Views.EventView.EventView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ghenterprise.Views.EventView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:models="using:Ghenterprise.Models"
    mc:Ignorable="d">
    <Grid>
        <controls:AdaptiveGridView
                        animations:Connected.ListItemElementName="itemThumbnail"
                        animations:Connected.ListItemKey="animationKeyContentGrid"
                        DesiredWidth="720"
                        ItemHeight="540"
                        IsItemClickEnabled="True"
                        ItemClickCommand="{x:Bind EventViewModel.ItemClickCommand}"
                        ItemsSource="{x:Bind EventViewModel.Source, Mode=OneWay}"
                        SelectionMode="None"
                        StretchContentForSingleRow="False" Margin="{StaticResource LargeLeftTopRightBottomMargin}">
            <controls:AdaptiveGridView.ItemTemplate>
                <DataTemplate x:DataType="models:Event">
                    <Grid
                        x:Name="itemThumbnail"
                        Background="{ThemeResource CardBackground}"
                        CornerRadius="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="2*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <controls:ImageEx Grid.Row="0" IsCacheEnabled="True" Source="/Assets/example.jpg" Stretch="UniformToFill" />
                        <StackPanel                                     
                            Padding="{StaticResource SmallLeftTopRightBottomMargin}"
                            Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <TextBlock  x:Name="Title"
                                        Margin="{StaticResource XXSmallTopMargin}"
                                        HorizontalAlignment="Left"
                                        FontSize="{StaticResource MediumFontSize}"
                                        FontWeight="SemiBold"
                                        Text="{x:Bind Name}"                                       
                                        TextWrapping="WrapWholeWords"/>

                            <TextBlock  x:Name="EnterPrise"
                                        Margin="{StaticResource XXSmallTopMargin}"
                                        HorizontalAlignment="Left"
                                        FontSize="{StaticResource SmallFontSize}"
                                        FontWeight="SemiBold"
                                        Text="ViewModel Enterprise Naam"
                                        TextWrapping="WrapWholeWords"/>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <FontIcon Glyph="&#xEE93;" 
                                                      FontSize="{StaticResource SmallFontSize}" 
                                                      Margin="{StaticResource XSmallLeftTopRightBottomMargin}"
                                                      Foreground="{ThemeResource SystemAccentColor}"/>
                                <TextBlock
                                                FontSize="{StaticResource CardTextFontSize}"
                                                Margin="{StaticResource XXSmallTopMargin}"
                                                HorizontalAlignment="Left"
                                                Foreground="{StaticResource SystemBaseMediumColor}">
                                    <Run>VAN </Run>
                                    <Run>VAN </Run>
                                    <Run Text="{x:Bind StartDate}"></Run>
                                    <Run> TOT </Run>
                                    <Run Text="{x:Bind EndDate}"></Run>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Text="{x:Bind Description}" FontSize="{StaticResource CardTextFontSize}" Margin="{StaticResource SmallTopMargin}" TextWrapping="WrapWholeWords"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </controls:AdaptiveGridView.ItemTemplate>
        </controls:AdaptiveGridView>
    </Grid>
</Page>
