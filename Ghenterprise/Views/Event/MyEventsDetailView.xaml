<Page
    x:Class="Ghenterprise.Views.Event.MyEventsDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ghenterprise.Views.Event"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
>
    <ScrollViewer
        Name="ForegroundElement"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        Padding="{StaticResource SmallLeftTopRightBottomMargin}"
        HorizontalAlignment="Stretch"
        VerticalScrollMode="Enabled">
        <StackPanel Name="block" Padding="0,15,0,0">
            <TextBlock Text="{x:Bind MasterEventItem.Id, Mode=OneWay}" />
            <TextBlock Text="{x:Bind MasterEventItem.Name, Mode=OneWay}" />
            <TextBlock Text="{x:Bind MasterEventItem.Description, Mode=OneWay}" />
            <TextBlock Text="{x:Bind MasterEventItem.StartDate, Mode=OneWay}" />
            <TextBlock Text="{x:Bind MasterEventItem.EndDate, Mode=OneWay}" />
        </StackPanel>
    </ScrollViewer>
</Page>
